Brand merch icon set (hand-drawn marks): Heart, Smile, Coffee, SmileyHappyPlus. Render by name.

```jsx
<Icon name="Heart" size={28} />
<Icon name="CoffeeSize24" />
<Icon name="Smile" size={32} style={{ color: 'var(--lal-vermelho)' }} />
```

See `Icon.d.ts` for the full name list. Single-colour marks paint with `currentColor`.
