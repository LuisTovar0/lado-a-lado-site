Top navigation: script logo on the left, centred uppercase mono links, outline action button.

```jsx
<NavBar links={["Sobre","Oradores","Horário","Reserva"]} cta="Inscrição" />
```
On dark surfaces pass `logoColor="var(--lal-bege)"`.
